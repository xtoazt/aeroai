# Environment variables for Unrestricted AI Assistant
# No API keys needed - OpenRouter key is hardcoded for unrestricted access

# Optional: Debug mode
DEBUG=false

# Note: This application uses OpenRouter with a pre-configured API key
# for unrestricted AI access. No additional API keys are required.
